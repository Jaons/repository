#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    pid_t   pid;
    FILE    *fd;
    
    if ()
    
    if (pid = fork() < 0) {
        printf("fork error %s\n", strerror(errno));
        return (errno);
    }
    
    else if (pid == 0){
        
    }
    
}
